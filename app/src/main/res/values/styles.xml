<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/md_teal_800</item>
        <item name="colorPrimaryDark">@color/md_teal_900</item>
        <item name="colorAccent">@color/md_teal_50</item>
        <item name="elevation">0dp</item>
    </style>
    <style name="AppTheme.fullscreen" parent="Theme.AppCompat.Light.NoActionBar"></style>
</resources>
